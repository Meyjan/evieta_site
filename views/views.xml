<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="evieta_site.list">
      <field name="name">evieta_site list</field>
      <field name="model">evieta_site.evieta_site</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="evieta_site.action_window">
      <field name="name">evieta_site window</field>
      <field name="res_model">evieta_site.evieta_site</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="evieta_site.action_server">
      <field name="name">evieta_site server</field>
      <field name="model_id" ref="model_evieta_site_evieta_site"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="evieta_site" id="evieta_site.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="evieta_site.menu_1" parent="evieta_site.menu_root"/>
    <menuitem name="Menu 2" id="evieta_site.menu_2" parent="evieta_site.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="evieta_site.menu_1_list" parent="evieta_site.menu_1"
              action="evieta_site.action_window"/>
    <menuitem name="Server to list" id="evieta_site" parent="evieta_site.menu_2"
              action="evieta_site.action_server"/>
    -->
  </data>
</odoo>